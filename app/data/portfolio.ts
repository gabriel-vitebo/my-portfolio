import type { PortfolioData } from '~/types/portfolio'
import { projects, projectsSection } from './projects'
import { hero, socials } from './profile'

export const portfolio: PortfolioData = {
  hero,
  socials,
  projectsSection,
  projects,
}
